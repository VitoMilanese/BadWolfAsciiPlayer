<Window x:Class="BadWolfAsciiPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BadWolf ASCII Player"
        Width="1180"
        Height="760"
        MinWidth="820"
        MinHeight="560"
        Background="#111111"
        Foreground="#F2F2F2"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver"
        Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="3"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Template Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#484848"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,4"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      IsHitTestVisible="False"/>
                                    <Border Grid.Column="1" BorderBrush="#444444" BorderThickness="1,0,0,0">
                                        <Path Width="8" Height="5"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Fill="#D8D8D8"
                                              Data="M 0 0 L 8 0 L 4 5 Z"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <ToggleButton Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Margin="0,2,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="260"
                                        Background="#262626"
                                        BorderBrush="#555555"
                                        BorderThickness="1"
                                        CornerRadius="2">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#777777"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#8A8A8A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#777777"/>
                                <Setter TargetName="OuterBorder" Property="Background" Value="#1B1B1B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Margin" Value="6,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button DockPanel.Dock="Left" Click="Open_Click">Open video</Button>
            <TextBlock x:Name="FileNameText" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#BEBEBE" Text="Drop an MP4/MKV/AVI/MOV here, or click Open video"/>
            <TextBlock x:Name="StatusText" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#E17878" Margin="12,0,0,0"/>
        </DockPanel>

        <Border Grid.Row="1" BorderBrush="#333333" BorderThickness="1" Background="Black" CornerRadius="4">
            <Grid>
                <Image x:Name="AsciiImage" Stretch="Uniform" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock x:Name="DropHint" Text="DROP VIDEO HERE" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32" FontWeight="SemiBold" Foreground="#3D3D3D" IsHitTestVisible="False"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="PlayPauseButton" Grid.Column="0" Width="80" Click="PlayPause_Click" IsEnabled="False">Play</Button>
            <TextBlock x:Name="PositionText" Grid.Column="1" VerticalAlignment="Center" Margin="8,0" Text="00:00 / 00:00"/>
            <Slider x:Name="SeekSlider" Grid.Column="2" Minimum="0" Maximum="1" Value="0" IsEnabled="False" PreviewMouseDown="SeekSlider_PreviewMouseDown" PreviewMouseUp="SeekSlider_PreviewMouseUp"/>
            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Vol" VerticalAlignment="Center"/>
                <Slider x:Name="VolumeSlider" Width="90" Minimum="0" Maximum="1" Value="0.8" ValueChanged="VolumeSlider_ValueChanged"/>
            </StackPanel>
        </Grid>

        <WrapPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Mode" VerticalAlignment="Center" Margin="6,0,2,0"/>
            <ComboBox x:Name="ModeCombo" Width="110" SelectionChanged="RenderSetting_Changed">
                <ComboBoxItem Content="Color" IsSelected="True"/>
                <ComboBoxItem Content="Mono"/>
            </ComboBox>
            <TextBlock Text="Columns" VerticalAlignment="Center" Margin="12,0,2,0"/>
            <ComboBox x:Name="ColumnsCombo" Width="90" SelectionChanged="RenderSetting_Changed">
                <ComboBoxItem Content="80"/>
                <ComboBoxItem Content="120"/>
                <ComboBoxItem Content="160" IsSelected="True"/>
                <ComboBoxItem Content="200"/>
                <ComboBoxItem Content="240"/>
            </ComboBox>
            <TextBlock Text="FPS" VerticalAlignment="Center" Margin="12,0,2,0"/>
            <ComboBox x:Name="FpsCombo" Width="80" SelectionChanged="RenderSetting_Changed">
                <ComboBoxItem Content="15"/>
                <ComboBoxItem Content="24"/>
                <ComboBoxItem Content="30" IsSelected="True"/>
                <ComboBoxItem Content="60"/>
            </ComboBox>
        </WrapPanel>
    </Grid>
</Window>
